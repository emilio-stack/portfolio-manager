export class Project {
    constructor(public title: string, public description: string, public image: string,
        public tags: string[], public source: string, public demo: string, public id: string) { }
}